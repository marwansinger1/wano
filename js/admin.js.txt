// ===== سكريبت لوحة تحكم الأدمن (Wano) =====

// متغير لتخزين حالة الإعلانات
let adsEnabled = false;

// دالة لتفعيل أو تعطيل الإعلانات
function toggleAds() {
  adsEnabled = !adsEnabled;
  if (adsEnabled) {
    alert('✅ تم تفعيل الإعلانات في الموقع');
    // هنا تقدر تضيف كود إظهار الإعلانات
  } else {
    alert('🚫 تم تعطيل الإعلانات في الموقع');
    // هنا تقدر تخفي الإعلانات
  }
}

// دوال أخرى مستقبلًا:
// - إضافة منتج جديد
// - تعديل منتج
// - حذف منتج
// (ممكن نوسعها لو حبيت بعدين 🔥)
